# network Communication Configuration

network_comm_node:
  ros__parameters:
    host: "0.0.0.0"  # Listen on all interfaces
    port: 5000
    telemetry_rate: 10.0  # Hz (telemetry updates)
    timeout_seconds: 2.0  # Connection timeout

video_stream_node:
  ros__parameters:
    udp_port:           5002   # UDP port rover → GUI for video frames
    default_quality:    70     # JPEG quality used when GUI sends quality=0 (1-100)
    stream_scale:       100    # Server-side spatial scale % applied to all streams (1-100)
                               # Reduce to 50 to halve resolution and cut bandwidth ~4x
    depth_max_m:        8.0    # DA3 depth clip distance (meters)
    orbbec_depth_max_m: 5.0    # Orbbec depth clip distance (meters)
