# Copy this file to .env and fill in your values before running the container.

# Windows host IP as seen from WSL2.
# From inside WSL2, run: ip route show default | awk '{print $3}'
ISAAC_SIM_HOST=172.x.x.x

# Must match the ROS_DOMAIN_ID set in Isaac Sim (default is 0).
ROS_DOMAIN_ID=0
